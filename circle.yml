checkout:
  post:
    - git submodule sync
    - git submodule update --init
    - mvn clean
    - mvn package
    - cp -R ./target/Nukkit-1.0.jar $CIRCLE_ARTIFACTS

machine:
  java:
    version: oraclejdk8
